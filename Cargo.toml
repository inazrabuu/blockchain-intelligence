[package]
name = "blockchain-intelligence"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = {version = "1", features = ["full"] }

[workspace]
members = [
  "ingestion-service",
  "api-service",
  "shared"
]